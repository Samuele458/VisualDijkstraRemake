import AddNodeIcon from "./icons/add.png";

const GraphToolbar = () => {
  return (
    <div className="graph-toolbar">
      <img src={AddNodeIcon} alt="Add new node" />
    </div>
  );
};

export default GraphToolbar;
